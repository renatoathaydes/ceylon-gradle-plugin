subprojects {
    buildscript {
        repositories {
            flatDir {
                dirs file( '../../build/libs' )
            }
            jcenter()
        }

        dependencies {
            classpath "com.athaydes.gradle.ceylon:ceylon-gradle-plugin:1.0"
        }
    }
}